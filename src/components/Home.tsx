import { Link } from 'react-router-dom';

export const Home = () => {
    return (
        <div>
            <h1>Diff Viewer</h1>
            <Link to="/diff">Diff Viewer</Link>
        </div>
    );
};
